{
  "name": "intelligent-data-dictionary-frontend",
  "version": "1.0.0",
  "description": "Frontend dependencies for visualization (if needed)",
  "scripts": {
    "test": "echo "Error: no test specified" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
