{
  "name": "intelligent-data-dictionary-backend",
  "version": "1.0.0",
  "description": "Backend helper scripts (if needed)",
  "scripts": {
    "test": "echo "Error: no test specified" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
